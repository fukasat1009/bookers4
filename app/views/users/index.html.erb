<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<% @users.each do |user| %>
<%= attachment_image_tag user, :profile_image, fallback:"no-image.png" %>
<%= user.name %>
<%= link_to "Show", user_path(user), class:"button is-warning is-light" %>
<% end %>